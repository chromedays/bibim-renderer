{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Fastbuild (Debug)",
            "type": "shell",
            "command": "./FBuild.exe Debug",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Fastbuild (Release)",
            "type": "shell",
            "command": "./FBuild.exe Release",
            "problemMatcher": [],
            "group": "build"
        }
    ]
}